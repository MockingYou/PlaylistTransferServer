package com.app.transfer.domain;

public enum StreamingProvider {
    SPOTIFY,
    YOUTUBE
    // APPLE_MUSIC, DEEZER, etc. added later — this is the ONLY enum you touch to register a new provider name
}